head
	title Оценки
	meta(charset="utf-8")
	meta(name="viewport" content="width=device-width, initial-scale=1")
	link(rel="shortcut icon" href="images/favicon.ico" type="image/x-icon")
	link(rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css")
	style
		include ../public/style/admin.css
	script(src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js")
	script(src="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js")
	script
		include ../public/scripts/jquery-3.2.1.min.js
	script
		include ../public/scripts/js_marks.js
	script(src="http://cdnjs.cloudflare.com/ajax/libs/jquery.form/3.51/jquery.form.min.js")
body
	div(class="header")
		p Пользователь: #{user}
		a(href="/login" )
			button(type="button" class="btn btn-link" id="exit")  Выход
	div(class="container")
		div( id="menu")
			a(href="/profile" class="btn btn-primary" id="current") Главная
			a(href="/schedule/teacher" class="btn btn-primary") Расписание    
			a(href="/up/teacher" class="btn btn-primary") Учебный план    
			a(href="/marks/teacher" class="btn btn-primary")  Оценки
			a(href="/msg" class="btn btn-primary")  Сообщения
	h2 Здесь Вы можете просмотреть и выставить итоговые оценки по предметам 
	p
	div(class="row")
		div(class="col")
			select(name='group' id='group_change' class="form-control")
				option(disabled selected) Выберите группу
				- var c = groups
				each item in c
					option(value=item.id)=item.name
		div(class="col")
			select(name='dis' id='sem_change' class="form-control")
				option(disabled selected) Выберите семестр
		div(class="col")
			select(name='dis' id='dis_change' class="form-control")
				option(disabled selected) Выберите дисциплину
	
	div(id="table")
	button(type='button' id='submarks' class="btn btn-primary") Ок